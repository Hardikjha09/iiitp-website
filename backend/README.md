# Backend — Express API

Node.js + Express + Prisma + MySQL backend for the IIIT Pune CMS.

## Setup

```bash
npm install
npx prisma generate
npx prisma migrate dev
```

See `BACKEND_IMPLEMENTATION_PLAN.md` in the root for the full architecture and phased roadmap.
